{
  "name": "chat-app",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "devDependencies": {
    "artillery": "^2.0.0"
  },
  "scripts": {
    "artillery:login": "npx artillery run artillery/login-cookie-test.yaml",
    "artillery:room": "npx artillery run artillery/room-auth-test.yaml"
  }
}
